import React from "react";
import ReactDOM from "react-dom/client";
import DiscordBotDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DiscordBotDashboard />
  </React.StrictMode>
);
